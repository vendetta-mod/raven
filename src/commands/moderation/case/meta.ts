import { ICommand } from "../../../handlers/command"

export default {
    name: "case",
    description: "Various utilities for moderation cases.",
    requiredPermissions: ["ModerateMembers"],
} as ICommand;
